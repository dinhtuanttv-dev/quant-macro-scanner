# Ghim chính xác phiên bản để tránh Vercel build ra bản vnstock khác,
# có thể đổi cách gọi hàm bất cứ lúc nào (v4 Unified UI còn đang phát triển
# nhanh).
#
# SU CO PyPI (2026-09-25, QUAN TRONG): toan bo package "vnstock" VA
# "vnai" (dependency BAT BUOC cua vnstock v4.x) tren PyPI da bi PyPI
# admin dua vao "Quarantine" tu 24/09/2026 ("cannot be installed or
# modified until a security review is complete" - xac nhan qua
# https://pypi.org/project/vnstock/#history VA https://pypi.org/
# project/vnai/). KHONG THE cai qua PyPI. "vnai" CUNG KHONG CO ma
# nguon GitHub cong khai (xac nhan qua PyPI support issue #12390:
# "Source code repositories URLs: none") - khong the ap dung cach cai
# tu GitHub nhu vnstock.
#
# GIAI PHAP 2 PHAN:
#  1. vnstock: cai THANG tu GitHub, ghim tag on dinh GAN NHAT da xac
#     nhan TON TAI THAT qua "git ls-remote --tags" (khong co tag rieng
#     cho 4.0.5-4.0.8) la v4.0.4.
#  2. vnai: DA DOC TRUC TIEP source code vnstock (tag v4.0.4) va xac
#     nhan CHI dung DUY NHAT 2 thu tu vnai - optimize_execution()
#     (decorator factory tren cac ham API) va setup() (goi 1 lan luc
#     khoi tao, DA duoc chinh vnstock boc trong try/except nen AN
#     TOAN du loi/thieu). vnai THUC CHAT la thu vien TELEMETRY/
#     ANALYTICS PHU TRO (theo mo ta chinh thuc), KHONG PHAI logic loi
#     lay du lieu chung khoan. Da tu viet 1 package STUB local (NO-OP,
#     khong gui telemetry that) tai api/_vendor/vnai_stub/ - xem chi
#     tiet + ly do tai api/_vendor/vnai_stub/vnai/__init__.py. Dong
#     duoi tro thang vao stub nay bang DUONG DAN TUYET DOI
#     (/vercel/path0/api/_vendor/vnai_stub) - da thu relative path
#     ("./_vendor/vnai_stub") truoc do nhung uv (khac pip) tu nhan doi
#     "api/" trong qua trinh resolve, gay loi "Distribution not found
#     at .../api/api/_vendor/..." - duong dan tuyet doi nay CHINH XAC
#     tu chinh thong bao loi thuc te cua Vercel build log.
#
# KHI PyPI het quarantine (theo doi https://pypi.org/project/vnstock/
# va https://pypi.org/project/vnai/): co the go 2 dong duoi + xoa thu
# muc api/_vendor/vnai_stub/, doi lai "vnstock==<ban moi>" (pip resolve
# vnai tu dong theo dependency that cua vnstock).
vnai @ file:///vercel/path0/api/_vendor/vnai_stub
vnstock @ git+https://github.com/thinh-vu/vnstock.git@v4.0.4
fastapi==0.117.1
pandas==2.2.3
python-dateutil==2.9.0

# THEM (Cluster Panel - HDBSCAN) - LUU Y QUAN TRONG: da thu tach rieng
# requirements.txt cho api/cluster/ (thu muc con), nhung gap loi 404 vi
# Vercel KHONG map "api/cluster/index.py" -> "/api/cluster" nhu ky vong
# (khac quy uoc file phang "api/stock.py" -> "/api/stock" da xac nhan
# hoat dong). Da chuyen ve file phang "api/cluster.py" - dung CHUNG file
# requirements.txt nay voi stock.py. Anh huong THAT: kich thuoc goi trien
# khai cua stock.py se lon hon (hdbscan+numpy+scipy+scikit-learn duoc cai
# vao chung environment), co the lam cold start hoi cham hon truoc mot
# chut - NHUNG code stock.py khong import cac goi nay nen KHONG anh huong
# toc do CHAY logic cua no, chi anh huong thoi gian tai bundle luc cold
# start. Day la danh doi da duoc thong bao ro cho nguoi dung.
hdbscan==0.8.44
pydantic==2.9.2
