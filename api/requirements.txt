# Ghim chính xác phiên bản để tránh Vercel build ra bản vnstock khác,
# có thể đổi cách gọi hàm bất cứ lúc nào (v4 Unified UI còn đang phát triển
# nhanh).
#
# SU CO PyPI (2026-09-25, QUAN TRONG): toan bo package "vnstock" tren
# PyPI (MOI phien ban, khong rieng 4.0.5/4.0.8) da bi PyPI admin dua
# vao "Quarantine" tu 24/09/2026 ("cannot be installed or modified
# until a security review is complete" - xac nhan qua https://pypi.org/
# project/vnstock/#history). KHONG THE cai qua PyPI bang BAT KY cach
# ghim phien ban nao cho toi khi PyPI hoan tat rieng soat.
#
# GIAI PHAP: cai THANG tu GitHub (README chinh thuc cua vnstock cung
# huong dan cach nay cho "experimental release": "pip install
# git+https://github.com/thinh-vu/vnstock.git"). Da kiem tra truc tiep
# qua "git ls-remote --tags" - KHONG co tag rieng cho 4.0.5-4.0.8 tren
# GitHub (tac gia co the chua tag cac ban nay), tag on dinh GAN NHAT
# DA XAC NHAN TON TAI la v4.0.4 - ghim dung tag nay (KHONG dung @main,
# tranh nhan code chua on dinh/thay doi bat ngo).
#
# NEU PyPI da het quarantine VA muon quay lai ghim phien ban binh
# thuong (nhanh hon, khong can build tu source): doi dong duoi thanh
# "vnstock==<phien_ban>" (kiem tra https://pypi.org/project/vnstock/#history
# truoc, dam bao KHONG con bao "Quarantined" o dau trang).
vnstock @ git+https://github.com/thinh-vu/vnstock.git@v4.0.4
fastapi==0.117.1
pandas==2.2.3
python-dateutil==2.9.0

# THEM (Cluster Panel - HDBSCAN) - LUU Y QUAN TRONG: da thu tach rieng
# requirements.txt cho api/cluster/ (thu muc con), nhung gap loi 404 vi
# Vercel KHONG map "api/cluster/index.py" -> "/api/cluster" nhu ky vong
# (khac quy uoc file phang "api/stock.py" -> "/api/stock" da xac nhan
# hoat dong). Da chuyen ve file phang "api/cluster.py" - dung CHUNG file
# requirements.txt nay voi stock.py. Anh huong THAT: kich thuoc goi trien
# khai cua stock.py se lon hon (hdbscan+numpy+scipy+scikit-learn duoc cai
# vao chung environment), co the lam cold start hoi cham hon truoc mot
# chut - NHUNG code stock.py khong import cac goi nay nen KHONG anh huong
# toc do CHAY logic cua no, chi anh huong thoi gian tai bundle luc cold
# start. Day la danh doi da duoc thong bao ro cho nguoi dung.
hdbscan==0.8.44
pydantic==2.9.2
